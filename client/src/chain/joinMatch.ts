// Phase 2 TODO: build and submit the join_match Anchor instruction.
// Returns the transaction signature so the server can verify on-chain membership.
export async function joinMatchOnChain(_matchPDA: string): Promise<string | null> {
  console.log("[joinMatch] on-chain join deferred to Phase 2 — skipping");
  return null;
}
